const roleUpgrader = {
    run: function(creep) {
        // 实现升级者的逻辑
        if (creep.store[RESOURCE_ENERGY] > 0) {
            // 升级控制器
        } else {
            // 找能源
        }
    }
};

module.exports = roleUpgrader;
