const roleBuilder = {
    run: function(creep) {
        // 实现建筑者的逻辑
        if (creep.store[RESOURCE_ENERGY] > 0) {
            // 建造建筑
        } else {
            // 找能源
        }
    }
};

module.exports = roleBuilder;
