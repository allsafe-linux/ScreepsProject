const roleHarvester = {
    run: function(creep) {
        // 实现采集者的逻辑
        if (creep.store.getFreeCapacity() > 0) {
            // 采集能源
        } else {
            // 运输能源
        }
    }
};

module.exports = roleHarvester;
